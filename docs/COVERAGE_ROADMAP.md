# Cobertura de tests — roadmap

**Versión**: v0.1
**Última medición**: 2026-07-13 — **92.85%** (checkout limpio) sobre 6.912 statements en `app/`. Gate `make cov` = **91%** — **hito DoD v0.1 (≥70%) ALCANZADO** (margen +22.85). Histórico: 24.09% (2026-05-24) → 25.28% (Ciclo 1) → 29.37% (Ciclo 2) → 39.06% (Ciclo 3, gate 37%) → 43.11% (Ciclo 4, gate 41%) → 44.60% (Ciclo 5, gate 43%) → 45.51% (Ciclo 6, gate 44%) → 48.99% (Ciclo 7, gate 47%) → 51.93% (Ciclo 8, gate 49%: `google_calendar.py` 13→98%) → 53.31% (Ciclo 9, gate 51%: `event_bus.py` 0→100%) → 57.31% (Ciclo 10, gate 55%: `markdown_sync.py` 0→100%) → 59.90% (Ciclo 11, gate 57%: `mcp_generator.py` 0→100%) → 61.05% (Ciclo 12, gate 59%: `entire_session.py` 0→100% — wrapper del CLI `entire`) → 61.62% (Ciclo 13, gate 60%: `workflow_engine.py` 81→100% — aristas del grafo (agente/acción desconocidos), budget de retries en bucle, camino de fallo con `next_on_failure`, `except` tragados de checkpoint/end_session, trimming de `_runs`, y `_build_messages` por acción; workflows custom vía `monkeypatch.setitem(WORKFLOWS, ...)`) → 61.87% (Ciclo 14, gate 61%: `user_store.py` 88→100% + `prompt_store.py` 95→100% — método `initialize()` DB-setup (`create_async_engine`/`async_sessionmaker`/`create_all`), `close()` y guard de `_session()`; fake de engine con `begin()` async-CM + `run_sync` AsyncMock, sin Postgres real) → 65.47% (Ciclo 16, gate 65%: `api/v1/prompts.py` 0→93% — router CRUD/notes/retry/classify/stage/promote/lists/pipeline sobre `FastAPI()` local + `AsyncMock` `prompt_store` en `app.state`; incluye fix de ruta `/prompts/lists` oculta por `/{prompt_id}`) → **67.85%** (Ciclo 17, gate 67%: `api/v1/routine.py` 0→99% — helpers puros `_parse_routine_md`/`_compute_event_times`/`_tz_offset_for` (frontmatter tipado, descripción multilínea, tz DST-aware + fallback) + endpoints `GET /today` y `POST /sync` con `ROUTINE_FILE` redirigido a fixture temporal, `get_google_calendar` fakeado y `prompt_store` `AsyncMock`; sin Google Calendar/DB/red reales) → **71.58%** (Ciclo 18, gate 70: `api/v1/mcp.py` 0→100% + `api/v1/skills.py` 0→100% — MCP: `get_mcp_generator` monkeypatcheado a `MagicMock` síncrono (singleton real nunca construido, sin disco) + `_parse_prompt_to_spec` puro directo; Skills: `AsyncMock` de manager en `app.state` + ramas fallback de `_get_manager` (503 sin prompt_store, cache de singleton, RuntimeError→503) vía `GET /skills/{slug}`; quirk fijado: `list_skills`/`create_skill` afloran el 503 como 500 por falta de `except HTTPException: raise` — **hito DoD v0.1 ≥70% ALCANZADO**) → **73.18%** (Ciclo 20, gate 72: `api/v1/calendar.py` 0→100% — router Google Calendar (auth OAuth/callback/status/calendars/events GET+POST/sync/disconnect) sobre `FastAPI()` local con `get_google_calendar` monkeypatcheado a `MagicMock` (métodos async = `AsyncMock`, síncronos `get_status`/`is_connected` = returns planos); todas las ramas 200/400/403/500/501 + 422 de validación + propagación del 400 de fecha inválida vía `except HTTPException: raise`; singleton real nunca construido, sin OAuth/token/red) → **74.26%** (Ciclo 21, gate 73: `api/v1/dashboard.py` 0→100% — endpoint agregador `GET /dashboard/summary` sobre `FastAPI()` local con `app.state` (prompt_store/executor/agent/scheduler/google_calendar) fijado a `AsyncMock`/`MagicMock` y `get_policy_engine` monkeypatcheado; cubiertas las 6 sub-agregaciones y sus ramas: dependencia ausente (defaults/[]), store con dicts vs no-dict vs `Exception`, budget del engine vs `except`→ceros, agentes presentes/ausentes, calendario conectado/no-conectado/`Exception`/eventos vacíos; sin infra, red ni `.env`) → **75.17%** (Ciclo 22, gate 74: `api/v1/agents.py` 0→100% — router multi-agente (crews list/create, workflows, execute, runs list+detalle) sobre `FastAPI()` local con `app.state.crew_manager`/`workflow_engine` fijados a `MagicMock`/`AsyncMock`; cubiertos ambos helpers `_get_*`→503 por dependencia ausente, `create_crew` ValueError→400 + validación 422, rama `status=="failed"`→`log.warning` de `execute`, proyección de campos de `list_runs` con eco de `limit`/`offset` + validación `le=200`→422, `get_run` 404, y auth 401/403; `WORKFLOWS` real (5 defs) ejercitado sin mock; sin infra, red ni `.env`) → **75.67%** (Ciclo 23, gate 74 sin cambio: `api/v1/budget.py` 0→100% — router de presupuesto/políticas (`GET /status`, `PATCH /limits`, `POST /category-policy`, `POST /evaluate`) sobre `FastAPI()` local con `get_policy_engine` monkeypatcheado a un `MagicMock` síncrono (import a nivel de módulo → se parchea el nombre en `app.api.v1.budget`; singleton real nunca construido); `evaluate` devuelve un `PolicyDecision` real para que la proyección quede JSON-serializable; cubiertas las ramas 200/400 (política inválida)/422 (body incompleto), assert de kwargs `daily`/`monthly` de `set_budget` (body completo/vacío/parcial), las 5 políticas válidas parametrizadas, eco de query en `evaluate`, y auth 401/403; sin infra, red ni `.env`. Ratchet **no-op**: `floor(75.67)−1 = 74`, se necesitaría ≥76% para subir a 75) → **76.14%** (Ciclo 24, gate **74→75**: `api/v1/tunnel.py` 0→100% — router ngrok (`GET /status`, `POST /start`, `POST /stop`, `GET /info`) sobre `FastAPI()` local con `get_tunnel_service` monkeypatcheado (import a nivel de módulo → nombre parcheado en `app.api.v1.tunnel`; singleton real nunca construido, pyngrok jamás importado); `start`/`stop` = `AsyncMock`, `get_info` = `MagicMock` síncrono, `is_connected`/`public_url` atributos planos; cubiertas las 3 ramas de `/start` (ya-conectado→early return sin await, éxito con URL→`{success,public_url}`, fallo→500) con plumbing del port (body `{port}`/sin-body→`None`/body vacío→`None`), ambas ramas de `/stop` (no-conectado→early return sin await, conectado→await `stop`), `/status` y `/info` delegando a `get_info`, y auth 401/403 parametrizada en los 4 endpoints; sin infra, red ni `.env`. Ratchet **efectivo**: `floor(76.14)−1 = 75`) → **76.94%** (Ciclo 25, gate 75 sin cambio: `api/v1/sync.py` 0→100% + `api/v1/audit.py` 0→100% — **cierra el backlog de routers a 0%**. Sync (patrón `app.state`): fake `md_sync` inyectado en `app.state` (`get_status` síncrono, `full_sync`/`get_today_inbox_md` = `AsyncMock`); cubiertas las ramas "not initialized" de los 3 endpoints (`/status`→disabled, `/run`→error sin await de `full_sync`, `/inbox`→503) + happy paths (`get_status` delegado, `full_sync` awaited + `result` reflejado, markdown devuelto tal cual) + fallback de `/inbox` parametrizado (`""`/`None`→"No captured prompts today"). Audit (import a nivel de módulo, como `budget.py`): `get_entire_service` monkeypatcheado a `MagicMock` síncrono; cubiertos `/status`, `/sessions` (defaults + eco `limit`/`offset` con assert de kwargs), `/sessions/{id}` (encontrado + `None`→404), `/prompts/{id}/sessions`, y auth 401/403 parametrizada. Ambos routers 100%, 0 miss; sin infra, red ni `.env`. Ratchet **no-op**: `floor(76.94)−1 = 75`, se necesitaría ≥77% para subir a 76) → **80.44%** (Ciclo 26, gate **75→79**: `app/services/osascript.py` 24→**100%** — mayor bloque descubierto del proyecto (318 stmts), primer módulo de *servicio* cubierto tras cerrar el backlog de routers. Todo el módulo envuelve `subprocess.run` + parseo de stdout, así que es 100% testeable sin macOS real: los tests de helpers/verify parchean `osa.subprocess.run` para las 4 ramas de `_run_applescript`/`_run_applescript_file` (éxito con strip, rc≠0→`OSAScriptError` con stderr, `TimeoutExpired`, `Exception` genérica) y las 3 ramas de `_verify_osascript` (rc0 ok, rc≠0→warning, excepción→warning, ninguna propaga); los ~30 métodos públicos parchean el helper de instancia (`_run_applescript`/`_run_applescript_file`) devolviendo string canned o lanzando `OSAScriptError`, aislando parseo (`split("|")`/`"|||"`/`", "`), degradación (`[]`/`None`/`False`/`""`), clamp de `set_volume` (0–100), escape de `create_note` (`"`→`\"`, `\n`), `missing value`→`None` en contactos, y assert del AppleScript construido (nombre de calendario/lista/carpeta, `subtitle`, `make new tab`); +78 tests, sin infra, red ni `.env`. Ratchet **efectivo**: `floor(80.44)−1 = 79`, salto de +3.50 puntos, el mayor de un ciclo) → **81.09%** (Ciclo 27, gate **79→80**: cierre de **huecos residuales** en 8 módulos near-100% — primer ciclo sin bloques descubiertos grandes. `agents/prompt_os_agents.py` 98→100% (brace-block con JSON inválido → `except json.JSONDecodeError`), `context_assembler.py` 97→100% (dos `except Exception` de lectura de `cowork.md`/lista activa vía dir→`IsADirectoryError`), `frangels/quota_manager.py` 97→100% (`_load` JSON corrupto + `_save` write→dir + filtros `require_vision`/`require_tools` que hacen `continue`), `google_calendar.py` 98→100% (guard `except ImportError` del import opcional vía `importlib.reload` con `google.*` bloqueado en `sys.modules` + restauración), `prompt_agent.py` 98→100% (`group_id` truthy→`correlation_id`, `scheduled_at`/`created_at` naive→coerción UTC), `service_registry.py` 97→100% (hint `else`→`make docker-full` con >1 unhealthy + `except Exception` del loop de monitoring con backoff), `frangels/angels.py` 97→100% (helpers `get_inference_angels`/`get_available_angels` en nuevo `test_angels_codex.py`), `scheduler.py` 99→100% (`except ImportError` de `_sync_calendar`). +16 tests, sin infra/red/`.env`. Ratchet **efectivo**: `floor(81.09)−1 = 80`) → **82.57%** (Ciclo 28, gate **80→81**: `app/api/v1/frangels.py` 36→**100%** (187 stmts, 120 miss cerrados) — el mayor router sin cubrir, primer router del eje frangels/providers. Montado sobre `FastAPI()` local con los tres singletons por import a nivel de módulo monkeypatcheados en el namespace del router (`get_provider_store`/`get_quota_manager` = `MagicMock` síncrono; `get_frangels_orchestrator` con `chat`/`test_provider` = `AsyncMock`); `ANGEL_REGISTRY`/`AngelCategory`/`PrivacyLevel` reales, `QuotaStatus`/`InferenceResult` construidos reales para que `asdict`/proyección JSON queden serializables. Cubiertos los 15 endpoints y sus ramas: `/providers` (ternario quota presente/None + summary + by_category), `/providers/{id}` (404 + asdict con/sin quota), POST `/providers` (400 desconocido/400 key vacía/422 + assert `set` con `strip`), DELETE (404 + happy), toggle (404/400 no-configurado/enable/disable), test (404/happy/`except`→`success:false` no-500), `/usage`, `/quotas` (summary exhausted+warning), `/quotas/{id}` (404/happy), `/chat` (happy proyección usage/message + `PrivacyLevel` válido→coerción/inválido→`except ValueError`/error→502), `/chat/available-providers` (side_effect de cred/`can_use` para los 3 `continue` + orden por tier), `/status` (available_by_category), `/sync-env` (delta before/after), `/export-env` (masking >8 vs ≤8 chars), y auth 401/403 parametrizada. +38 tests, sin infra/red/`.env`. **Medición honesta:** 82.57% en checkout limpio (fichero suelto ajeno `test_api_prompts_codex.py` stasheado antes de medir; con él daría 82.83%). Ratchet **efectivo**: `floor(82.57)−1 = 81`) → **87.57%** (Ciclo 29, gate **81→86**: `app/api/v1/system.py` 35→**100%** (414 stmts, 270 miss cerrados) — **el mayor bloque descubierto que quedaba en el proyecto**, router de integración macOS/OSASCRIPT (28 endpoints). Montado sobre `FastAPI()` local con el singleton `osascript_service` (import a nivel de módulo) monkeypatcheado en el namespace del router a un `MagicMock` **síncrono** (ningún método se `await`ea); `audit_logger` **real** degrada solo (lee `getattr(request.app.state,'event_store',None)` → sin `event_store` en la app local no toca disco/DB/red); auth **real** con key `{"all"}`. Cubiertas las 3 formas de endpoint: lectura GET (happy + `except OSAScriptError`→500, params opcionales sanitizados con/sin valor, `/music/current` track truthy/None), escritura POST (`success`→200 / falsy→500 + 422 de body incompleto), y high-risk con `OSAScriptSecurityContext` (`/volume`,`/dark-mode/toggle`,`/safari/open`,`/finder/reveal`,`/clipboard`: happy/500/**403** vía `settings.osascript_disabled_operations`); + **validadores de modelos** (subtitle/location/notes con `""` explícito para que Pydantic v2 ejecute el `return v` del branch falsy, sound/voice fuera de whitelist→fallback, clamp de volumen >100→100, `validate_url` inválido→422); + ramas cross-cutting (**503** con `osascript_enabled=False`, **400** de `validate_path` fuera de prefijos permitidos, **401** con `osascript_require_auth` forzado a True → `verify_api_key` levanta antes del rate-limit). +80 tests, sin infra/red/`.env`. **Medición honesta:** 87.57% en checkout limpio (el mismo fichero suelto ajeno `test_api_prompts_codex.py` stasheado antes de medir; con él daría 87.83%). Ratchet **efectivo**: `floor(87.57)−1 = 86`, salto de +5.00 puntos, el mayor de un ciclo) → **88.86%** (Ciclo 30, gate **86→87**: `app/api/v1/energy.py` 33→**100%** (133 stmts, 89 miss cerrados) — el mayor router descubierto que quedaba, sistema de energía inteligente (batería/solar/red + decisiones de cómputo). Helpers puros probados directos: `get_battery_status` con `energy.subprocess.run` mockeado (5 ramas de parseo de `pmset`: `\d+%` presente/ausente→fallback 100, `AC Power`/`charging`→is_charging, `H:MM remaining`→minutos/−1, `power_source` ac/battery/unknown, `except`→dict fallback); `calculate_state` las 5 ramas SURVIVAL/CRITICAL/CONSERVING/ABUNDANT/NORMAL; `get_recommendations` los 5 estados. Endpoints sobre `FastAPI()` local con `AsyncClient`: `/status` (NORMAL/CONSERVING/CRITICAL vía `get_battery_status` parcheado + `settings.solar_api_url` None/valor→`solar_available`), `/compute-recommendation` (las 5 ramas — ABUNDANT/SURVIVAL forzadas parcheando `calculate_state`, pues los endpoints fijan `is_online=True`/`solar_watts=0.0` y no son alcanzables por battery), `/history` (`app.state.event_store` None→"not available" vs `AsyncMock.query_events`→eventos con eco de `hours` y assert de kwargs category/subcategory/limit). WebSocket `/ws`: `TestClient` síncrono con `dependency_overrides[verify_auth]` (el `APIKeyHeader` de `verify_auth` no resuelve en scope WS) + `get_battery_status` con `side_effect=[dict, RuntimeError]` y `energy_check_interval=0` → una iteración envía un `energy_update` y el `except/finally` cierra limpio. Auth 401/403 parametrizada en los 3 GET. +30 tests, sin `pmset`/macOS/subprocess/infra/red/`.env`. **Medición honesta:** 88.86% en checkout limpio (mismo fichero suelto ajeno `test_api_prompts_codex.py` stasheado antes de medir). Ratchet **efectivo**: `floor(88.86)−1 = 87`) → **90.02%** (Ciclo 31, gate **87→89**: `app/sdk/client.py` 59→**100%** (197 stmts, 80 miss cerrados) — **primer módulo del eje SDK**, el cliente `IdmServiceClient` que los 6 satélites usan para integrarse con Micelia. Cubierto en `tests/test_sdk_client_codex.py` (+20 tests, todo in-process con `AsyncMock` de http/redis; sin red/infra/subprocess): lifecycle `start()`/`stop()` con recursos vivos, `_heartbeat_loop` (happy + recuperación de excepción), ramas de error de `publish_event`/`publish_event_bus`, `subscribe` (sin redis→`RuntimeError`, resolución de shorthand, listener), `_listen_loop` (dispatch async/sync, JSON inválido→raw, callback que lanza, `Cancelled`/excepción genérica), `health` uptime, propiedad deprecada `idm_core_url`, y `_connect_redis` (éxito/`ImportError`/fallo de conexión). **Nota de proceso:** el commit del test (`81fd20e`) aterrizó en una ejecución previa del mismo día pero sin sus commits de contabilidad (`chore(cov)` + `docs(log)`); esta ejecución los completa. **Medición honesta:** 90.02% en checkout limpio (fichero suelto ajeno `test_api_prompts_codex.py` stasheado antes de medir). Ratchet **efectivo**: `floor(90.02)−1 = 89`) → **90.74%** (Ciclo 32, gate 89 sin cambio: `app/api/v1/gateway.py` 60→**100%** (70 stmts, 28 miss cerrados) + `app/api/v1/events.py` 68→**100%** (69 stmts, 22 miss cerrados) — **cierra los dos routers parciales más grandes que quedaban**. Gateway: `proxy_request` sobre `FastAPI()` local con `app.state.http_client`=`AsyncMock` (import directo, no `getattr`) — reproduce status/headers/content del upstream, filtra `host`/`content-length`, body solo en POST/PUT/PATCH, params de query, ruta a cada `*_service_url`; unknown-service→404 (`SERVICE_ROUTES` monkeypatcheado a `{}`); traducción de errores httpx `Timeout`→504/`ConnectError`→503/genérico→502; `research_to_course_pipeline` con `education_service_enabled` on (3 llamadas get/post/post + `event_store.append_event` awaited) / off (course `None`, 1 POST) / `event_store` None (salta append) / upstream lanza→500. Events (patrón `app.state.event_store`, import directo): `None`→503 en los 5 endpoints que lo exigen; happy de `list_events` (eco `limit`/`offset` + assert de kwargs de filtros; `limit` `le=1000`→422), `get_timeline` (fecha válida con/sin `categories`, inválida→400), `get_by_correlation` (UUID válido / inválido→422), `create_event` (proyección `event_id`/`status`/`timestamp` + assert kwargs incl. `event_metadata`/`tags`; body incompleto→422), `get_event_stats` (eco `period_days` + `since`≈`now−days`); `list_categories` estático (4 categorías); auth 401/invalid-key. +41 tests (24 events + 17 gateway), sin red/infra/subprocess/`.env`. **Medición honesta:** 90.74% en checkout limpio (mismo fichero suelto ajeno stasheado). Ratchet **no-op**: `floor(90.74)−1 = 89`, se necesitaría ≥91% para subir a 90) → **91.72%** (Ciclo 33, gate **89→90**: `app/events/store.py` 47→**100%** (128 stmts, 68 miss cerrados) — **primer módulo del eje de persistencia/event-store**, el `EventStore` async (event sourcing append-only sobre PostgreSQL/SQLAlchemy). Cubierto en `tests/test_events_store_codex.py` (+22 tests) con `AsyncSession` **mockeada** (MagicMock/AsyncMock), replicando el patrón de `test_prompt_store_codex.py` (Ciclo 14) — **sin Postgres, sin dependencias nuevas** (`aiosqlite` NO instalado; se descartó por guardarraíl de no tocar `uv.lock`). `initialize()` con `create_async_engine`/`async_sessionmaker` monkeypatcheados + `conn.run_sync(create_all)` awaited (éxito + re-raise); `close()` (engine presente→`dispose` / `None`→no-op); `append_event()` (happy→`UUID` + `add`/`commit`, todos los campos, defaults `{}`/`[]`, y **rama legacy `source="idm-core"`→`"micelia"` + `DeprecationWarning`** vía `pytest.warns`); `query_events()` (sin filtros→rama `if conditions` falsa, todos los filtros→ramas verdaderas, vacío); `get_by_correlation()`, `get_timeline()` (con/sin `categories`, vacío), `get_stats()` (poblado, con `since`, vacío — dicts `by_category`/`by_source` desde `result.all()` de tuplas); `_event_to_dict()` (full + `correlation_id=None` + `timestamp=None`, ramas ternarias). Sin red/infra/subprocess/`.env`. **Medición honesta:** 91.72% en checkout limpio (mismo fichero suelto ajeno `test_api_prompts_codex.py` stasheado antes de medir). Ratchet **efectivo**: `floor(91.72)−1 = 90`) → **92.62%** (Ciclo 34, gate **90→91**: `app/core/security.py` 76→**100%** (257 stmts, ~62 miss cerrados) — **primer módulo del eje de seguridad/auth**, la superficie de autenticación de la plataforma (gestor de API-keys en memoria, rate limiter de ventana deslizante, sanitizador anti-inyección AppleScript, audit logger al Event Store, gestor JWT mono-usuario, y las dependencias FastAPI `verify_auth`/`verify_api_key`/`check_rate_limit`/`require_write_permission`/`osascript_security`/`verify_api_key_global` + el clasificador de riesgo `OSAScriptSecurityContext`). Cubierto en `tests/test_core_security_codex.py` (+52 tests) **todo in-process, sin infra/red/`.env`**: `Request` es un fake `SimpleNamespace` (solo `.client.host`/`.headers.get`/`.url.path`/`.method`/`.app.state`/`.state`), el Event Store un `AsyncMock` (o ausente → rama None-safe), `settings` monkeypatcheado por test, y los singletons de módulo `api_key_manager`/`rate_limiter` reemplazados por instancias frescas (fixture autouse) para evitar fuga de estado; JWT/bcrypt corren **de verdad** (jose + passlib), lo que además vigila la ruta cripto del funnel register→login. Cubiertas: `APIKeyManager` (`_load_default_keys` master+readonly + skip del placeholder/None, `validate_key` vacío/None/hit, `has_permission` all/specific/none, `generate_key`), `RateLimiter` (bajo/en límite, límite custom, `_cleanup_old_requests` con timestamps stale purgados, `get_limit_for_key` tier/default/desconocido), `AppleScriptSanitizer` (`sanitize_string` vacío/truncado/patrón peligroso→`ValueError`/escape/strip de control chars, `validate_url` vacío/no-http/localhost bloqueado-si-no-debug vs permitido-si-debug, `validate_path` vacío/traversal/prefijo no permitido/happy, `is_safe`), `AuditLogger` (`log_operation`/`log_security_event` × {store None, store OK awaited, store lanza→tragado}; masking de key corta `***` vs larga `pre...post`; `client` None→`unknown`), `JWTAuthManager` (verify sin hash→False, verify True/False con bcrypt real, `hash_password` marca `$2`, roundtrip access/refresh), y las dependencias (`verify_auth` apikey válida/inválida/bearer válido/tipo-erróneo/`JWTError`/nada; `verify_api_key` require-auth missing-401/invalid-401/valid + auth-off passthrough; `check_rate_limit` allowed-set-state vs exceeded-429 con headers; `require_write_permission` write/all/denegado-403/skips; `osascript_security(write=)` read vs write-403; `verify_api_key_global` missing/invalid/valid; `add_security_headers`; `OSAScriptSecurityContext` disabled/high-risk-prod/allow-flag/normal/`is_write_operation`/async-CM). **Medición honesta:** 92.62% en checkout limpio (mismo fichero suelto ajeno `test_api_prompts_codex.py` stasheado antes de medir; 1419 pass + 2 skip, +52 vs Ciclo 33). Ratchet **efectivo**: `floor(92.62)−1 = 91`) → **92.85%** (Ciclo 35, gate 91 sin cambio: `app/core/config.py` 92→**100%** (136 stmts, 11 miss cerrados) + `app/api/v1/health.py` 92→~92% con las ramas de estado cubiertas — **colas de bajo riesgo**. Config (`tests/test_core_config_codex.py`, +14 casos): `Settings` instanciado con kwargs explícitos + `_env_file=None` (kwargs > env > `.env` en pydantic-settings, así el `.env` real nunca se lee); cubiertos los dos validadores de aviso de seguridad (`secret_key`/`system_api_key` en default→`UserWarning` vía `pytest.warns`; no-default→sin warning con `simplefilter("error")`), `parse_cors_origins` (JSON-string→`json.loads` + passthrough de lista), `parse_disabled_operations` (None/`""`→`[]` + CSV con strip), `parse_allowed_paths` (None/`""`→defaults + CSV), `is_production` (production/development), la propiedad `services` (6 `ServiceConfig` con url/enabled/timeout reflejados) y el singleton `get_settings` cacheado. Health (`tests/test_api_health_branches_codex.py`, +5 casos): las ramas que `test_health.py` no alcanza, llamando los handlers directo con un `Request` fake (`SimpleNamespace`) y `service_registry`=`AsyncMock` — `readiness_check` "not_ready" (ambos críticos down) además del "ready"; y las 3 ramas de `overall_status` de `/detailed` (healthy=todos up, degraded=mixto vía `side_effect`, unhealthy=todos down). **Descubrimiento de proceso:** las colas de 503 de `prompts.py` (18 miss) que el log de Ciclo 34 recomendaba cerrar **ya están cubiertas** por las +44 líneas del fichero suelto sin committear `test_api_prompts_codex.py` — escribir tests nuevos las duplicaría, así que se dejó `prompts.py` intacto (ver DP en el log). +19 tests, sin infra/red/`.env`. **Medición honesta:** 92.85% en checkout limpio (mismo fichero suelto ajeno `test_api_prompts_codex.py` stasheado antes de medir; 1438 pass + 2 skip, +19 vs Ciclo 34). Ratchet **no-op**: `floor(92.85)−1 = 91`, se necesitaría ≥93% para subir a 92)

---

## Decisión

El gate de `make cov` en v0.1 es **25%** (baseline conservador justo por encima de la medición actual). Esto es intencionalmente bajo y honestamente reconocido.

**Por qué NO forzamos 70% en v0.1**:

- Subir de 24% → 70% requiere ejercitar ~4.700 statements adicionales con tests
- Eso es trabajo de 1-2 semanas dedicadas a escribir tests por encima de una superficie de código que aún está evolucionando
- Forzar el threshold ahora produciría dos malos resultados: (a) bloquear la release indefinidamente, o (b) inflar la cobertura con tests "de paso" sin valor real
- El estado real del proyecto se describe mejor publicando un baseline bajo y un compromiso explícito de subida progresiva

**Por qué 25% y no menos**:

- Asegura que la cobertura no retroceda accidentalmente respecto a lo que ya existe
- Es 0.91 puntos por encima de la medición actual (24.09%) — pasa hoy con margen pero detecta regresiones futuras

---

## Estado actual por módulo (orden de prioridad para subir cobertura)

Módulos críticos del orquestador identificados con baja cobertura. Cada uno representa una oportunidad de progreso medible hacia el objetivo v0.2.

### Alta prioridad (core funcional, alta superficie sin tests)

| Módulo | Razón | Plan v0.2 |
|---|---|---|
| `app/services/agents/` | Sistema multi-agente. Crítico operativamente, suite E2E no lo toca | ✅ crew_manager + agent_definitions + workflows (Ciclo 1), workflow_engine 81→**100%** (Ciclo 13: aristas de grafo + retries + audit except + `_build_messages`), prompt_os_agents 98% (Ciclo 4). Módulo cubierto |
| `app/services/frangels/` | Orquestación de providers IA. Crítico para AI sovereignty | ✅ policy_engine 100%, quota_manager 97%, angels 97% (Ciclo 2), provider_store 100% (Ciclo 4), orchestrator 100% (Ciclo 7: select_angel + rutas HTTP de todos los providers, mocks httpx). Módulo cubierto |
| `app/services/prompt_*` | Pipeline completo de prompts (store, agent, executor, scheduler) | ✅ executor 100%, scheduler 99%, agent 98% (Ciclos previos), store **100%** (Ciclo 6: lists/promote/inbox/close; `initialize` DB-setup cerrado en Ciclo 14). Pipeline cubierto |
| `app/services/user_store.py` | Capa de datos del funnel register/login | ✅ **100%** (Ciclo 4/14: CRUD email único + normalización + `initialize`/`close`/guard de sesión, mocks async-session sin Postgres) |
| `app/api/v1/ai.py` | Endpoint integración modelos. Tocado tangencialmente por T3.1 | Tests directos de cada endpoint con mocks de proveedor |
| `app/api/v1/prompts.py` | API de prompts. No cubierta por E2E | Tests CRUD básicos |

### Media prioridad

| Módulo | Razón | Plan v0.2 |
|---|---|---|
| ~~`app/api/v1/skills.py`, `mcp.py`, `agents.py`~~ | APIs especializadas | ✅ skills.py + mcp.py 0→100% (Ciclo 18), agents.py 0→100% (Ciclo 22: crews/workflows/execute/runs sobre `FastAPI()` local, `crew_manager`/`workflow_engine` mockeados). Cubiertas |
| `app/services/skills_manager.py` | Gestión dinámica de skills | Tests CRUD + activación |
| ~~`app/services/google_calendar.py`~~ | Integración externa | ✅ 13→98% (Ciclo 8: auth OAuth + load/save creds + calendars/events CRUD + sync bidireccional; mocks Flow/Credentials/build + PromptStore). Solo el fallback `except ImportError` de import queda sin cubrir |
| `app/services/tunnel.py` | ngrok wrapper | Tests con mock pyngrok |
| ~~`app/services/entire_session.py`~~ | Wrapper del CLI `entire` (trazabilidad de sesiones) | ✅ 0→100% (Ciclo 12: start/checkpoint/end_session con available on/off + `except`, getters, `_run_entire` rc 0/≠0, `_trim_sessions`, singleton; fake de `create_subprocess_exec` + `shutil.which` parcheado, sin subprocesos reales) |
| ~~`app/core/security.py`~~ | API key + rate limiting | ✅ 76→**100%** (Ciclo 34: APIKeyManager + RateLimiter + AppleScriptSanitizer + AuditLogger + JWTAuthManager + dependencias FastAPI de auth + `OSAScriptSecurityContext`; fake `Request` `SimpleNamespace` + `AsyncMock` event-store + JWT/bcrypt reales, sin infra) |

### Baja prioridad (cobertura indirecta o intencionalmente no testeada)

| Módulo | Razón |
|---|---|
| `app/cli.py` | CLI; sólo se ejercita en startup via `micelia start`. Tests requerirían subprocess + colaboración con uvicorn |
| `app/services/osascript.py` | macOS-specific, no portable a CI Linux |
| `app/main.py` lifespan | Ya cubierto por arranque de la suite E2E vía `_build_test_app` |

---

## Hoja de ruta cuantitativa

| Release | Threshold | Plan |
|---|---|---|
| **v0.1** | **25%** | Baseline honesto. Garantiza no regresión. (HOY) |
| **v0.2** | **45%** | Tests alta prioridad: agents/, frangels/, prompt_* |
| **v0.3** | **60%** | Tests media prioridad: APIs v1 + skills + integraciones externas con mocks |
| **v0.4** | **70%** | Subir prioridad baja + eliminar gaps residuales |
| **v1.0** | **80%** | Auditoría completa antes de release marca registrada |

Cada salto exige unos 5-10 PRs de tests específicos. El roadmap es honesto: 70% en v0.1 era objetivo aspiracional; 70% en v0.4 (con plan medible) es objetivo realista.

---

## Cómo contribuir tests para subir el baseline

1. Elegir un módulo de la sección "Alta prioridad" sin asignar
2. Abrir issue con el módulo elegido para evitar duplicidad de trabajo
3. Escribir tests que mockeen las fronteras externas (BD, HTTP, providers IA)
4. PR con un commit de tests + un commit que suba el `--cov-fail-under` del Makefile en el delta alcanzado
5. Revisión sigue el patrón de `docs/CLA.md` + `.github/PULL_REQUEST_TEMPLATE.md`

---

## Métricas de seguimiento

Cada PR debe reportar el delta de cobertura. Cuando se llegue a uno de los hitos del roadmap, actualizar este documento y el threshold del Makefile en el mismo PR.

Comando para medir local:

```bash
make cov     # corre con threshold actual
make cov --cov-fail-under=0   # corre sin gate (info-only)
```

Reporte HTML granular en `htmlcov/index.html` tras cualquier ejecución de `make cov`.
